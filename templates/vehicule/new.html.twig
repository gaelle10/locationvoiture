{% extends 'admin.html.twig' %}
{% block title %}Ajouter un véhicule{% endblock %}

{% block body %}
    <h1>Ajouter un véhicule</h1>

    <div class="card">
        <div class="card-body">
            {{ include('vehicule/_form.html.twig') }}
        </div>
    </div>

    <a class="btn btn-primary" href="{{ path('app_vehicule_index') }}">Tous les véhicules</a>
{% endblock %}
